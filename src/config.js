export const SCALE = 2;      // integer upscale factor (art stays low-res, text gets more pixels)
export const GAME_W = 320 * SCALE;   // 640
export const GAME_H = 180 * SCALE;   // 360

export const UI_STRIP_H = 50 * SCALE;  // Height of verb bar + inventory strip in Mode A
